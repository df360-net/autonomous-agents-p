# autonomous-agents

Before doing anything else, read [agent-reminder.md](agent-reminder.md) — it is the full context
handoff for this project: what we're building (a fleet of autonomous developer-agents tasked by
email, brain = the DeepSeek `agent.py`), the decisions already locked, the Zeenie runtime facts,
how the user works, and exactly where we are and what's next.
